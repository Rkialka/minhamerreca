[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA: qualquer rota cai no index.html (exceto /api/*, resolvido pelas functions)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
